<Window x:Class="DiplomAPM.RequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новая заявка" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="Transparent" AllowsTransparency="True" WindowStyle="None">

    <Border Background="White" CornerRadius="20" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Новое обращение" FontSize="22" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center"/>
                <TextBlock Text="Заполните данные заявки" FontSize="14" Foreground="#999" HorizontalAlignment="Center" Margin="0,5,0,20"/>
            </StackPanel>

            <StackPanel Grid.Row="1">

                <TextBlock Text="Заявитель (Гражданин)" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Height="40" Margin="0,0,0,15">
                    <ComboBox x:Name="cbCitizens" BorderThickness="0" Background="Transparent" 
                              DisplayMemberPath="FIO" SelectedValuePath="CitizenID"
                              VerticalContentAlignment="Center" Padding="10,0"/>
                </Border>

                <TextBlock Text="Категория проблемы" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Height="40" Margin="0,0,0,15">
                    <ComboBox x:Name="cbCategories" BorderThickness="0" Background="Transparent" 
                              DisplayMemberPath="CategoryName" SelectedValuePath="CategoryID"
                              VerticalContentAlignment="Center" Padding="10,0"/>
                </Border>

                <TextBlock Text="Суть проблемы" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Height="100" Margin="0,0,0,15">
                    <TextBox x:Name="txtDescription" BorderThickness="0" Background="Transparent" 
                             TextWrapping="Wrap" Padding="10" VerticalScrollBarVisibility="Auto"
                             FontSize="14"/>
                </Border>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Отмена" Click="BtnCancel_Click" Width="100" Height="45" Margin="0,0,10,0"
                        Background="#F1F2F6" Foreground="#777" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Сохранить" Click="BtnSave_Click" Width="140" Height="45"
                        Background="#6C5CE7" Foreground="White" BorderThickness="0" FontWeight="Bold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>